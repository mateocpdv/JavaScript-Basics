const numbers = [1, 2, 3, 4];

//End
// se agrega con → numbers.push();
// const last = numbers.pop();

// Beggining
// se agrega con → numbers.unshift();
// const firts = numbers.shift();

//Middle
// numbers.splice();

numbers.splice(2, 2);
console.log(numbers);

// ---------------------------------------------------
